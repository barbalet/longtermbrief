/**
 * sim_tile.js
 * In the C implementation, tiles pack multiple environmental signals.
 * In this port, tiles are derived on the fly from landSample().
 */
export function tileFromSample(sample) {
  return sample;
}
